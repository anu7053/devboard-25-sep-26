name: Node.js App Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Check for package.json
        shell: bash
        run: |
          if [ ! -f package.json ]; then
            echo "No package.json found. Skipping npm install and npm test."
            exit 0
          fi

      - name: Install dependencies
        if: hashFiles('package.json') != ''
        run: npm install

      - name: Run App Tests
        if: hashFiles('package.json') != ''
        run: npm test












































































































































































































































  

steps:

actions:
